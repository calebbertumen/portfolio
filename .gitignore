# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js
.next/
out/
build/

# Env & secrets (never commit real values)
.env
.env.*
!.env.example

# Debug / logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# OS
.DS_Store
Thumbs.db

# IDE / editor
.idea/
*.swp
*.swo

# Certificates & keys
*.pem
*.key
*.p12

# Cloud / service account keys (never commit)
*service-account*.json
*credentials*.json

# Vercel (may contain project linkage; optional to keep local only)
.vercel/

# Test coverage
coverage/

# TypeScript build cache
*.tsbuildinfo

# v0 sandbox / tooling (legacy)
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
next.user-config.*

# Optional: keep a resume PDF out of the repo (upload via hosting UI or release asset)
# public/resume.pdf
